import React from 'react';
import Topic from '../../../CustomComponents/Topic';

export default function NegativeAnswer() {
    const Grammar = () => {
        return <></>;
    }
    const descripcion = '';
    return <Topic name='Preguntas en negativo' grammar={Grammar} description={descripcion} />
}
